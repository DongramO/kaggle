# Core packages
numpy>=1.24.0
pandas>=2.0.0
scikit-learn>=1.3.0

# Model libraries
catboost>=1.2.0
lightgbm>=4.0.0
xgboost>=2.0.0

# Hyperparameter optimization
optuna>=3.0.0

# GPU support (optional - install if you have NVIDIA GPU)
# CUDA toolkit should be installed separately
# For XGBoost GPU: pip install xgboost --upgrade
# For LightGBM GPU: pip install lightgbm --config-settings=cmake.define.USE_GPU=ON
# For CatBoost GPU: GPU support is included by default if CUDA is available

# Note: GPU support requires:
# 1. NVIDIA GPU with CUDA support
# 2. CUDA toolkit installed
# 3. Appropriate GPU-enabled packages

