# Kaggle Competition - 모델 학습 파이프라인

이 프로젝트는 CatBoost, LightGBM, XGBoost 모델을 사용한 앙상블 학습 파이프라인입니다. Optuna를 활용한 하이퍼파라미터 최적화와 GPU 지원을 포함하고 있습니다.

## 📋 프로세스 개요

전체 학습 프로세스는 다음과 같은 단계로 구성됩니다:

```
1. 데이터 로드
   ↓
2. 데이터 전처리 및 Feature Engineering
   ↓
3. 하이퍼파라미터 최적화 (Optuna, 선택사항)
   ↓
4. 모델 학습 (K-Fold Cross-Validation)
   - CatBoost
   - LightGBM
   - XGBoost
   ↓
5. 앙상블 모델 생성 (가중치 최적화)
   ↓
6. 제출 파일 생성
```

## 🔄 상세 프로세스

### 1. 데이터 로드 (`load_data()`)
- **입력 파일**: 
  - `train.csv`: 학습 데이터 (피처 + 타겟)
  - `test.csv`: 테스트 데이터 (피처만)
  - `sample_submission.csv`: 제출 템플릿 (id 컬럼)
  
- **환경 자동 감지**:
  - Kaggle 환경: `/kaggle/input` 디렉토리 존재 여부로 판단
  - 로컬 환경: 프로젝트 내 `data/` 디렉토리 사용
  - 경로를 동적으로 조정하여 두 환경 모두 지원

- **출력**: 
  - `df_train`: 학습용 DataFrame
  - `df_test`: 테스트용 DataFrame  
  - `df_sub`: 제출용 템플릿 DataFrame

### 2. 데이터 전처리 (`prepare_data()`)

#### 2.1 기본 전처리
- **ID 컬럼 제거**: `id` 컬럼을 학습에서 제외 (식별자만 포함)
- **타겟 변수 분리**: 
  - `y_train = train['exam_score']` (회귀 타겟)
  - `X_train = train.drop(columns=['exam_score'])` (피처만)
  
#### 2.2 컬럼 타입 분류
- **수치형 컬럼** (`numeric_cols`): `int64`, `float64` 타입
  - 예: `age`, `study_hours`, `class_attendance`, `sleep_hours`
- **범주형 컬럼** (`categorical_cols`): `object`, `category` 타입
  - 예: `gender`, `course`, `internet_access`, `sleep_quality`, `study_method`, `facility_rating`

#### 2.3 Feature Engineering (`apply_feature_engineering_pipeline()`)

**이상치 클리핑** (`clip_outliers()`):
- **목적**: 극단값이 모델 성능에 악영향을 미치는 것을 방지
- **방법**: Quantile 기반 클리핑
- **클리핑 규칙** (기본값):
  ```python
  clip_rules = {
      'study_hours': 0.995,      # 99.5% 분위수로 클리핑
      'class_attendance': 0.99,   # 99% 분위수로 클리핑
      'sleep_hours': 0.995,       # 99.5% 분위수로 클리핑
      'age': 0.99                 # 99% 분위수로 클리핑
  }
  ```
- **원리**: 각 컬럼의 상위 0.5~1% 극단값을 해당 분위수 값으로 제한

**빈도 인코딩** (`create_frequency_features()`):
- **목적**: 범주형 변수의 빈도 정보를 수치형 특성으로 변환
- **생성 방식**: `{categorical_col}_freq` 컬럼 생성
- **예시**: `gender_freq`, `course_freq`, `study_method_freq`
- **특징**: 각 범주의 출현 빈도를 수치로 표현하여 모델이 학습 가능

**상호작용 특성** (`create_interaction_features()`):
- **목적**: 두 수치형 변수 간의 상호작용 효과 포착
- **생성 규칙** (현재 설정):
  ```python
  feature_pairs = [
      ('class_attendance', 'sleep_hours'),  # 수업 출석률 × 수면 시간
      ('study_hours', 'sleep_hours')        # 공부 시간 × 수면 시간
  ]
  operations = ['multiply']  # 곱셈만 생성
  ```
- **생성 특성**:
  - `class_attendance_x_sleep_hours`: 출석률과 수면 시간의 곱
  - `study_hours_x_sleep_hours`: 공부 시간과 수면 시간의 곱
- **추가 가능한 연산**: `multiply`, `divide`, `add`, `subtract` (현재는 곱셈만 사용)

**비율 특성** (`create_ratio_features()`, 현재 미사용):
- **목적**: 두 변수 간의 비율 관계 표현
- **예시**: `age / study_hours` (나이 대비 공부 시간 비율)

#### 2.4 범주형 인코딩 전략

**모델별 인코딩 차별화**:

1. **CatBoost용 (원본 범주형 유지)**
   - CatBoost는 범주형 변수를 자동으로 처리하므로 **원본 컬럼 유지**
   - `cat_features` 파라미터로 범주형 컬럼 인덱스 전달
   - 예: `gender`, `course`, `internet_access` 등 원본 그대로 사용

2. **LightGBM/XGBoost용 (인코딩 필요)**
   - Tree 기반 모델은 범주형 변수를 직접 처리하지 못하므로 인코딩 필수
   - **인코딩 방식**:
     - **One-Hot Encoding**: 설정 가능한 컬럼에 대해 사용 (기본값: `gender`, `course` 등)
       - 각 범주를 별도의 이진 컬럼으로 변환
       - 예: `gender_Male`, `gender_Female` (원본 컬럼 제거 가능)
     - **Ordinal Encoding**: 설정 가능한 컬럼에 대해 사용 (기본값: `exam_difficulty`)
       - 각 범주를 순서 있는 정수로 매핑
       - 예: `exam_difficulty`: `Easy=0`, `Medium=1`, `Hard=2`
       - `_encoded` 태그가 붙은 컬럼으로 저장 (원본 유지)

3. **인코딩된 컬럼 구분**:
   - 원본 범주형: CatBoost 전용
   - `*_encoded`: LightGBM/XGBoost 전용 (Ordinal)
   - `*_0`, `*_1`, ...: One-Hot 인코딩 결과

**인코딩 설정 예시**:
```python
ENCODING_CONFIG = {
    'onehot_cols': ['gender', 'course', 'internet_access', 'sleep_quality', 
                   'study_method', 'facility_rating', 'exam_difficulty'],
    'ordinal_cols': ['exam_difficulty'],
    'onehot_params': {'handle_unknown': 'ignore'},
    'ordinal_params': {'handle_unknown': 'use_encoded_value', 'unknown_value': -1},
    'drop_original': False  # 원본 컬럼 유지 (CatBoost용)
}
```

#### 2.5 결측치 처리
- **수치형 컬럼**: 해당 컬럼의 평균값으로 대체 (`fillna(mean_val)`)
- **범주형 컬럼**: 
  - One-Hot: `handle_unknown='ignore'`로 처리
  - Ordinal: `unknown_value=-1`로 처리
- **처리 순서**: Feature Engineering → 인코딩 → 결측치 처리

#### 2.6 최종 데이터 구조 및 특성 조합

**원본 특성 예시** (가정):
- 수치형: `age`, `study_hours`, `class_attendance`, `sleep_hours`
- 범주형: `gender`, `course`, `internet_access`, `sleep_quality`, `study_method`, `facility_rating`, `exam_difficulty`

**Feature Engineering 후 생성되는 특성**:
```
원본 수치형: age, study_hours, class_attendance, sleep_hours
↓
[이상치 클리핑] → 동일한 컬럼명, 값만 제한됨
↓
[빈도 인코딩] → gender_freq, course_freq, internet_access_freq, 
                sleep_quality_freq, study_method_freq, 
                facility_rating_freq, exam_difficulty_freq
↓
[상호작용 특성] → class_attendance_x_sleep_hours,
                  study_hours_x_sleep_hours
```

**인코딩 후 최종 특성 구성**:

**CatBoost 입력 특성** (예시):
```
수치형 원본:
  - age, study_hours, class_attendance, sleep_hours

Feature Engineering 결과:
  - gender_freq, course_freq, internet_access_freq, ...
  - class_attendance_x_sleep_hours, study_hours_x_sleep_hours

범주형 원본:
  - gender, course, internet_access, sleep_quality, 
    study_method, facility_rating, exam_difficulty

총 특성 수: 수치형(4) + FE 결과(9) + 범주형(7) = 약 20개
```

**LightGBM/XGBoost 입력 특성** (예시):
```
수치형 원본:
  - age, study_hours, class_attendance, sleep_hours

Feature Engineering 결과:
  - gender_freq, course_freq, internet_access_freq, ...
  - class_attendance_x_sleep_hours, study_hours_x_sleep_hours

Ordinal 인코딩된 범주형:
  - exam_difficulty_encoded (Easy=0, Medium=1, Hard=2)

One-Hot 인코딩된 범주형:
  - gender_Male, gender_Female
  - course_A, course_B, course_C, ...
  - internet_access_Yes, internet_access_No
  - sleep_quality_Good, sleep_quality_Fair, sleep_quality_Poor
  - study_method_Method1, study_method_Method2, ...
  - facility_rating_1, facility_rating_2, ..., facility_rating_5

총 특성 수: 수치형(4) + FE 결과(9) + Ordinal(1) + One-Hot(약 20-30개) = 약 35-45개
```

**특성 선택 로직 상세**:
- `get_features_for_model()` 함수가 모델 타입에 따라 적절한 컬럼만 선택
- CatBoost: 원본 범주형 포함, 인코딩된 컬럼 제외
- LightGBM/XGBoost: 인코딩된 컬럼 포함, 원본 범주형 제외

### 3. 하이퍼파라미터 최적화 (`optimize_hyperparameters()`)

#### 3.1 Optuna 최적화 (`use_optuna=True`)

**최적화 방법**:
- **Sampler**: TPE (Tree-structured Parzen Estimator)
  - 이전 trial의 결과를 바탕으로 다음 trial의 하이퍼파라미터를 지능적으로 선택
  - Random Search보다 효율적이며 Bayesian Optimization 방식
- **Cross-Validation**: 3-Fold (속도 최적화를 위해 5-fold에서 축소)
- **평가 지표**: 
  - Regression: `neg_mean_squared_error` (최소화)
  - Classification: `roc_auc` (최대화)

**모델별 하이퍼파라미터 최적화 범위**:

**CatBoost**:
```python
{
    'iterations': [100, 500],              # 부스팅 반복 횟수 (속도 최적화를 위해 2000→500으로 축소)
    'learning_rate': [0.01, 0.3] (log),    # 학습률 (로그 스케일)
    'depth': [4, 10],                      # 트리 깊이
    'l2_leaf_reg': [1, 100] (log),         # L2 정규화 계수 (로그 스케일)
    'random_strength': [0, 1],             # 랜덤성 강도
    'bagging_temperature': [0, 1],         # 배깅 온도 (Bayesian bootstrap일 때만)
    'bootstrap_type': ['Bayesian', 'Bernoulli', 'MVS']  # 부트스트랩 방식
}
```
- **특징**: Bootstrap type에 따라 `bagging_temperature` 파라미터 조건부 사용

**LightGBM**:
```python
{
    'num_leaves': [31, 255],               # 리프 노드 수
    'learning_rate': [0.01, 0.3] (log),    # 학습률 (로그 스케일)
    'max_depth': [3, 12],                  # 최대 트리 깊이
    'min_data_in_leaf': [20, 200],         # 리프 노드 최소 데이터 수
    'feature_fraction': [0.5, 1.0],        # 피처 샘플링 비율
    'bagging_fraction': [0.5, 1.0],        # 데이터 샘플링 비율
    'bagging_freq': [1, 7],                # 배깅 빈도
    'lambda_l1': [1e-3, 10.0] (log),       # L1 정규화 (로그 스케일)
    'lambda_l2': [1e-3, 10.0] (log)        # L2 정규화 (로그 스케일)
}
```

**XGBoost**:
```python
{
    'max_depth': [3, 12],                  # 최대 트리 깊이
    'learning_rate': [0.01, 0.3] (log),    # 학습률 (로그 스케일)
    'min_child_weight': [1, 10],           # 자식 노드 최소 가중치
    'subsample': [0.5, 1.0],               # 행 샘플링 비율
    'colsample_bytree': [0.5, 1.0],        # 열 샘플링 비율
    'gamma': [0, 1],                       # 최소 손실 감소량
    'reg_alpha': [1e-3, 10.0] (log),       # L1 정규화 (로그 스케일)
    'reg_lambda': [1e-3, 10.0] (log)       # L2 정규화 (로그 스케일)
}
```

**데이터 샘플링** (`optuna_sample_size`):
- 목적: 대용량 데이터에서 최적화 속도 개선
- 방법:
  - Classification: Stratified Sampling (클래스 비율 유지)
  - Regression: Random Sampling
- 예시: 10만 행 → 5만 행으로 샘플링하여 최적화 속도 2배 향상

**진행 상황 모니터링**:
- 1% 간격으로 Trial 진행 상황 출력
- Best Score와 Current Score 실시간 표시
- 예: `Trial 50/50 완료 | Best Score: 0.123456 | Current Score: 0.123789`

#### 3.2 저장된 파라미터 사용 (`use_saved_params=True`)
- 이전 Optuna 최적화 결과를 JSON 파일에서 로드
- 파일 형식: `best_hyperparameters.json`
- 각 모델별 최적 파라미터가 포함되어 있음

#### 3.3 기본 파라미터 사용 (`use_saved_params=False`, 파일 없음)
- 사전 정의된 기본 하이퍼파라미터 사용
- Optuna 최적화 없이 빠르게 학습 가능

### 4. 모델 학습 (`train_models()`)

각 모델에 대해 **5-Fold Cross-Validation**을 수행합니다:

#### 4.1 모델별 특성 사용 전략

**특성 컬럼 선택 로직** (`get_features_for_model()`):

1. **CatBoost**:
   ```python
   feature_cols = [모든 컬럼] - [인코딩된 컬럼(*_encoded)]
   ```
   - 사용: 원본 수치형 + 원본 범주형 + Feature Engineering 결과
   - 제외: One-Hot 인코딩 컬럼, Ordinal 인코딩 컬럼 (`*_encoded`)
   - 이유: CatBoost는 범주형을 자동 처리하므로 인코딩 불필요
   - `cat_features` 인덱스 리스트를 모델에 전달

2. **LightGBM/XGBoost**:
   ```python
   feature_cols = [모든 컬럼] - [원본 범주형 컬럼]
   ```
   - 사용: 수치형 + 인코딩된 범주형 (`*_encoded`) + One-Hot 컬럼 + Feature Engineering 결과
   - 제외: 원본 범주형 컬럼 (이미 인코딩됨)
   - 이유: Tree 기반 모델은 범주형을 직접 처리하지 못함

#### 4.2 CatBoost 학습 (`CatBoostModel`)

**특징**:
- **범주형 자동 처리**: 범주형 변수를 별도 인코딩 없이 직접 사용
- **GPU 지원**: `task_type='GPU'`, `devices='0'`
- **조기 종료**: Overfitting 방지를 위한 자동 조기 종료
- **목적 함수**: 
  - Regression: `RMSE`
  - Classification: `LogLoss`

**하이퍼파라미터** (최적화된 예시):
```python
{
    'iterations': 479,
    'learning_rate': 0.271,
    'depth': 7,
    'l2_leaf_reg': 30.58,
    'random_strength': 0.511,
    'bootstrap_type': 'Bernoulli'
}
```

**학습 과정**:
1. 각 Fold마다 학습 데이터로 모델 학습
2. `cat_features` 인덱스 전달로 범주형 처리
3. 검증 데이터로 예측 및 RMSE 계산
4. 테스트 데이터로 예측 (5개 Fold 모델의 평균)

#### 4.3 LightGBM 학습 (`LightGBMModel`)

**특징**:
- **빠른 학습 속도**: 히스토그램 기반 알고리즘으로 빠른 학습
- **메모리 효율적**: 큰 데이터셋도 효율적으로 처리
- **GPU 지원**: `device='gpu'`, `gpu_platform_id=0`, `gpu_device_id=0`
- **목적 함수**:
  - Regression: `rmse`
  - Classification: `binary_logloss`

**하이퍼파라미터** (최적화된 예시):
```python
{
    'num_leaves': 153,
    'learning_rate': 0.096,
    'max_depth': 11,
    'min_data_in_leaf': 90,
    'feature_fraction': 0.631,      # 63% 피처만 사용
    'bagging_fraction': 0.809,      # 81% 데이터만 사용
    'bagging_freq': 5,
    'lambda_l1': 0.021,
    'lambda_l2': 0.002
}
```

**학습 과정**:
1. 인코딩된 데이터로 학습 (범주형은 이미 수치화됨)
2. Early Stopping 사용 (검증 데이터 기준)
3. Feature Fraction과 Bagging으로 과적합 방지

#### 4.4 XGBoost 학습 (`XGBoostModel`)

**특징**:
- **강력한 정규화**: L1, L2 정규화로 과적합 방지
- **GPU 지원**: `tree_method='gpu_hist'`, `device='cuda:0'`
- **자동 폴백**: GPU 사용 불가 시 자동으로 CPU로 전환
- **목적 함수**:
  - Regression: `reg:squarederror`
  - Classification: `binary:logistic`

**하이퍼파라미터** (최적화된 예시):
```python
{
    'max_depth': 9,
    'learning_rate': 0.069,
    'min_child_weight': 10,
    'subsample': 0.843,              # 84% 행 샘플링
    'colsample_bytree': 0.822,       # 82% 열 샘플링
    'gamma': 0.190,
    'reg_alpha': 0.364,              # L1 정규화
    'reg_lambda': 0.106              # L2 정규화
}
```

**학습 과정**:
1. DMatrix 형식으로 데이터 변환
2. 인코딩된 데이터로 학습
3. 검증 데이터로 조기 종료 적용
4. GPU 실패 시 자동으로 CPU로 전환

#### 4.5 Cross-Validation 프로세스

**5-Fold CV 구조**:
```
전체 데이터 → [Fold 1, Fold 2, Fold 3, Fold 4, Fold 5]
각 Fold마다:
  - Train Set (80%): 모델 학습
  - Validation Set (20%): 성능 평가
  - Test Set: 최종 예측
```

**출력 결과**:
- **OOF (Out-of-Fold) 예측값**: 각 샘플이 validation set에 속했을 때의 예측값
  - 5개 Fold 모델의 예측을 결합하여 전체 학습 데이터에 대한 예측 생성
- **CV Score**: 각 Fold의 성능 평균 및 표준편차
  - Regression: RMSE 평균 ± 표준편차
  - Classification: AUC 평균 ± 표준편차
- **Fold별 점수**: 각 Fold의 개별 성능 확인 가능

**예측 통합**:
- **테스트 예측**: 5개 Fold 모델의 예측값을 평균하여 최종 예측
- **OOF 예측**: 앙상블 가중치 최적화에 사용

### 5. 앙상블 모델 생성 (`create_ensemble()`)

#### 5.1 앙상블 방법

**Weighted Average (가중 평균)**:
- 각 모델의 예측값에 가중치를 곱하여 합산
- 공식: `pred_ensemble = w1*pred_catboost + w2*pred_lightgbm + w3*pred_xgboost`
- 제약 조건: `w1 + w2 + w3 = 1` (가중치 합 = 1)

#### 5.2 가중치 최적화 (`_optimize_weights()`)

**최적화 방법**:
- **입력**: OOF 예측값 (3개 모델) + 실제 타겟값
- **목표**: 
  - Regression: 가중 평균 예측값과 실제값의 RMSE 최소화
  - Classification: 가중 평균 예측값과 실제값의 AUC 최대화
- **알고리즘**: Scipy의 `minimize` 함수 사용
- **제약 조건**: 
  - 가중치 합 = 1
  - 각 가중치 ≥ 0 (음수 가중치 방지)

**최적화 과정**:
1. 각 모델의 개별 성능 확인 및 출력
2. 초기 가중치: 각 모델의 성능에 비례하여 설정
3. 반복 최적화: 목적 함수를 최소화/최대화하는 가중치 탐색
4. 최종 가중치: 최적화된 가중치 저장

**최적화된 가중치 예시**:
```python
{
    'catboost': 0.35,    # 35% 가중치
    'lightgbm': 0.40,    # 40% 가중치
    'xgboost': 0.25      # 25% 가중치
}
```

#### 5.3 최종 예측

**테스트 데이터 예측**:
- 각 모델의 테스트 예측값에 최적 가중치 적용
- 최종 앙상블 예측값 계산
- 제출 파일에 저장

### 6. 제출 파일 생성
- **출력**: `submission.csv` (Kaggle: `/kaggle/working/submission.csv`)
- **형식**: `id`, `exam_score` 컬럼 포함

## ⚙️ 주요 설정 옵션

### GPU 사용 (`USE_GPU`)
```python
USE_GPU = True  # GPU 사용 (CUDA 지원 필요)
```
- CatBoost: `task_type='GPU'`, `devices='0'`
- LightGBM: `device='gpu'`
- XGBoost: `tree_method='gpu_hist'`, `device='cuda:0'`
- GPU 사용 불가 시 자동으로 CPU로 전환

### Optuna 최적화 (`USE_OPTUNA`)
```python
USE_OPTUNA = True   # Optuna 최적화 사용
N_TRIALS = 50       # 최적화 시도 횟수
```
- 각 모델별로 `N_TRIALS`만큼 최적화 수행
- 속도 최적화: 3-fold CV, 작은 iterations 범위

### 데이터 샘플링 (`OPTUNA_SAMPLE_SIZE`)
```python
OPTUNA_SAMPLE_SIZE = 50000  # 5만 개만 샘플링
```
- 데이터가 많을 때(10만 행 이상) 속도 개선
- Classification: Stratified Sampling
- Regression: Random Sampling

### 저장된 파라미터 사용 (`USE_SAVED_PARAMS`)
```python
USE_SAVED_PARAMS = None  # 자동 감지
# None: 파일 있으면 사용, 없으면 기본 파라미터
# True: 반드시 파일 사용 (없으면 오류)
# False: 파일 있어도 사용하지 않음
```

## 📊 출력 결과

### 학습 결과 요약 (`training_summary.txt`)
- 실행 시간
- 환경 정보 (Kaggle/로컬)
- GPU 사용 여부
- 모델별 CV Score (평균, 표준편차, Fold별 점수)
- 앙상블 가중치
- 하이퍼파라미터 정보

### 하이퍼파라미터 파일 (`best_hyperparameters.json`)
- 모델별 최적 하이퍼파라미터
- 최적화 메타데이터 (n_trials, random_state 등)

## 🚀 실행 방법

### 로컬 환경
```python
# main.py 실행
python main.py

# 또는 kaggle_submission.py 직접 실행
python kaggle_submission.py
```

### Kaggle Notebook
```python
# kaggle_submission.py 파일을 업로드한 후
exec(open('/kaggle/working/kaggle_submission.py').read())
```

## 📁 주요 파일 구조

```
202601_playground/
├── kaggle_submission.py      # Kaggle 제출용 통합 스크립트 (모든 코드 포함)
├── main.py                    # 로컬 환경 실행용 메인 파일
├── modeling/
│   ├── model.py              # 모델 클래스 정의
│   └── train.py              # 학습 로직
├── data/
│   ├── train.csv
│   ├── test.csv
│   └── sample_submission.csv
├── best_hyperparameters.json # 최적화된 하이퍼파라미터 저장 파일
├── submission.csv            # 최종 제출 파일
└── training_summary.txt      # 학습 결과 요약
```

## 🔧 주요 클래스 및 함수

### `HyperparameterOptimizer`
- `optimize_catboost()`: CatBoost 하이퍼파라미터 최적화
- `optimize_lightgbm()`: LightGBM 하이퍼파라미터 최적화
- `optimize_xgboost()`: XGBoost 하이퍼파라미터 최적화

### `ModelTrainer`
- `train_with_cv()`: K-Fold Cross-Validation으로 모델 학습
- `predict_test()`: 테스트 데이터 예측
- `create_model()`: 모델 생성 (CatBoost/LightGBM/XGBoost)

### `EnsembleModel`
- `fit()`: 앙상블 가중치 최적화
- `predict()`: 앙상블 예측 수행

## 💡 성능 최적화 팁

1. **속도 개선**:
   - Optuna 최적화 시 `OPTUNA_SAMPLE_SIZE` 사용 (대용량 데이터)
   - GPU 사용 (`USE_GPU = True`)
   - Optuna 최적화는 3-fold CV 사용 (기본 학습은 5-fold)

2. **정확도 개선**:
   - Optuna 최적화로 더 많은 trials 수행 (`N_TRIALS` 증가)
   - 더 많은 데이터 사용 (샘플링 크기 증가)
   - Feature Engineering 강화

3. **하이퍼파라미터 재사용**:
   - 한 번 최적화한 파라미터를 저장하여 재사용
   - `USE_SAVED_PARAMS = True`로 설정

## 📝 참고사항

- **Kaggle 환경 감지**: `/kaggle/input` 디렉토리 존재 여부로 자동 감지
- **범주형 처리**: CatBoost는 원본 사용, LightGBM/XGBoost는 인코딩 필요
- **메모리 관리**: 대용량 데이터는 샘플링 사용 권장
- **GPU 자동 전환**: GPU 사용 불가 시 자동으로 CPU로 전환

## 🐛 문제 해결

### GPU 사용 불가
- CUDA 설치 확인: `nvidia-smi` 명령어로 확인
- GPU 지원 패키지 설치 필요
- 자동으로 CPU로 전환되므로 오류 없이 실행 가능

### 메모리 부족
- `OPTUNA_SAMPLE_SIZE`로 데이터 크기 제한
- Feature Engineering 단계에서 불필요한 컬럼 제거

### Optuna 최적화 시간이 너무 오래 걸림
- `N_TRIALS` 감소
- `OPTUNA_SAMPLE_SIZE` 설정
- GPU 사용 (`USE_GPU = True`)

## 📈 특성 상관관계 및 다중공선성 분석

### 분석 도구 사용법

#### 1. 전처리된 데이터 분석 (권장)
실제 모델 학습에 사용되는 전처리된 데이터로 분석:

```python
# analyze_features.py 실행
python analyze_features.py
```

**생성되는 결과물**:
- `eda_results/correlation_heatmap_preprocessed.png`: 상관관계 히트맵
- `eda_results/high_correlations_preprocessed.csv`: 높은 상관관계 특성 쌍
- `eda_results/vif_preprocessed.csv`: VIF (다중공선성) 분석 결과
- `eda_results/vif_preprocessed.png`: VIF 막대 그래프
- `eda_results/target_correlations_preprocessed.csv`: 타겟 변수와의 상관관계
- `eda_results/target_correlation_preprocessed.png`: 타겟 상관관계 그래프
- `eda_results/catboost_high_correlations.csv`: CatBoost 특성 간 높은 상관관계
- `eda_results/lgbm_xgb_high_correlations.csv`: LightGBM/XGBoost 특성 간 높은 상관관계

#### 2. 원본 데이터 분석
전처리 전 원본 데이터로 분석:

```python
# eda/correlation_analysis.py 실행
python eda/correlation_analysis.py
```

**분석 항목**:
1. **상관관계 히트맵**: 모든 수치형 특성 간의 상관계수 시각화
2. **높은 상관관계 특성 쌍**: 임계값(0.6, 0.7, 0.8, 0.9) 이상의 상관관계를 가진 특성 쌍 탐색
3. **VIF (Variance Inflation Factor)**: 다중공선성 측정
   - VIF < 5: 다중공선성 낮음 (양호)
   - 5 ≤ VIF < 10: 다중공선성 보통 (주의)
   - VIF ≥ 10: 다중공선성 높음 (문제, 제거 고려)
4. **타겟 변수와의 상관관계**: 각 특성과 타겟 변수의 상관계수 분석

### 분석 결과 해석

#### 상관관계 해석
- **|r| < 0.3**: 약한 상관관계
- **0.3 ≤ |r| < 0.7**: 중간 상관관계
- **0.7 ≤ |r| < 0.9**: 높은 상관관계 (특성 선택 시 주의)
- **|r| ≥ 0.9**: 매우 높은 상관관계 (중복 특성 가능성)

#### 다중공선성 해석
- **VIF < 5**: 다중공선성 문제 없음
- **5 ≤ VIF < 10**: 다중공선성이 있으나 허용 가능
- **VIF ≥ 10**: 다중공선성 문제, 특성 제거 또는 변환 고려

### 권장 사항

1. **높은 상관관계 (|r| ≥ 0.8) 특성 쌍 발견 시**:
   - 한 특성만 유지하거나
   - 두 특성을 결합하여 새로운 특성 생성 (예: 평균, 차이)

2. **높은 VIF (VIF ≥ 10) 특성 발견 시**:
   - 해당 특성 제거 고려
   - PCA를 통한 차원 축소
   - 정규화 또는 변수 변환

3. **타겟 변수와의 상관관계가 낮은 특성 (|r| < 0.1)**:
   - 특성 중요도와 함께 검토하여 제거 고려
   - 단, 비선형 관계가 있을 수 있으므로 모델 성능 확인 필요

## 🔬 데이터 분석 및 개선 방향

### 현재 구현된 Feature Engineering

1. **이상치 처리 (Clipping)**:
   - 수치형 변수의 극단값을 분위수 기반으로 제한
   - 적용 컬럼: `study_hours`, `class_attendance`, `sleep_hours`, `age`
   - **개선 가능**: IQR 기반 이상치 처리, Z-score 기반 이상치 제거

2. **빈도 인코딩**:
   - 범주형 변수의 빈도를 수치형 특성으로 변환
   - **개선 가능**: Target Encoding (타겟 변수를 이용한 인코딩), Leave-One-Out Encoding

3. **상호작용 특성**:
   - 두 변수 간의 곱셈 연산만 수행
   - 현재: `class_attendance × sleep_hours`, `study_hours × sleep_hours`
   - **개선 가능**: 
     - 더 많은 변수 쌍 조합 시도
     - 나눗셈, 덧셈, 뺄셈 연산 추가
     - 3개 이상 변수의 다항 상호작용

4. **비율 특성**:
   - 현재 미사용 상태
   - **개선 가능**: 의미 있는 비율 특성 추가 (예: `study_hours / sleep_hours`)

### 추가로 개선 가능한 Feature Engineering

#### 1. 통계적 특성 생성
- **그룹별 통계량**: 
  - 범주형 변수별 수치형 변수의 평균, 표준편차, 최댓값, 최솟값
  - 예: `course별 study_hours 평균`, `gender별 age 표준편차`
- **순위 특성**: 
  - 수치형 변수의 순위 변환
  - 예: `study_hours_rank`

#### 2. 시간/순서 관련 특성 (해당되는 경우)
- **주기성 특성**: 
  - 시간 데이터가 있다면 요일, 월, 계절 등 추출
- **누적 특성**: 
  - 과거 데이터가 있다면 누적 합, 이동 평균 등

#### 3. 범주형 인코딩 고도화
- **Target Encoding**: 
  - 타겟 변수의 평균값으로 범주 인코딩
  - Overfitting 방지를 위해 Cross-Validation 사용 필요
- **Embedding**: 
  - 신경망 기반 임베딩 (CatBoost의 자동 처리와 유사하지만 더 유연)

#### 4. 특성 선택 (Feature Selection)
- **상관관계 분석**: 
  - 타겟과의 상관관계가 낮은 특성 제거
  - 특성 간 다중공선성 확인 및 처리
- **중요도 기반 선택**: 
  - 모델의 Feature Importance를 활용하여 중요도가 낮은 특성 제거
  - Permutation Importance 사용

#### 5. 데이터 증강 (Data Augmentation)
- **SMOTE (Classification)**: 
  - 소수 클래스 샘플 증강
- **Noise Injection**: 
  - 수치형 변수에 약간의 노이즈 추가로 일반화 성능 향상

### 모델 개선 방향

#### 1. 다양한 모델 추가
- **Neural Network**: 
  - TabNet, NODE (Neural Oblivious Decision Ensembles)
  - 범주형 변수 처리에 강점
- **Stacking**: 
  - Level 1: CatBoost, LightGBM, XGBoost
  - Level 2: Meta-learner (Linear Regression, Ridge 등)

#### 2. 하이퍼파라미터 최적화 개선
- **더 넓은 탐색 공간**: 
  - 현재 iterations 100-500 → 최종 학습 시 더 큰 범위 사용
  - Optuna 최적화와 최종 학습의 파라미터 범위 분리
- **Pruning**: 
  - Optuna의 Median Pruner 사용으로 성능이 낮은 trial 조기 종료
  - 속도 개선 가능

#### 3. Cross-Validation 전략 개선
- **Stratified K-Fold**: 
  - 타겟 변수의 분포를 고려한 분할 (Regression에서는 quantile-based stratification)
- **Time Series Split**: 
  - 시간 순서가 있는 경우 시간 기반 분할

### 데이터 분석을 위한 개선 사항

#### 1. EDA (Exploratory Data Analysis) 강화
- **분포 시각화**: 
  - 각 변수의 히스토그램, 박스플롯
  - 타겟 변수와의 관계 시각화
- **상관관계 분석**: 
  - 상관관계 히트맵
  - 다중공선성 확인
- **결측치 패턴 분석**: 
  - 결측치가 있는 행의 특징 분석
  - 결측치가 무작위인지 패턴이 있는지 확인

#### 2. 특성 중요도 분석
- **모델별 특성 중요도**: 
  - 각 모델의 Feature Importance 시각화
  - 공통으로 중요한 특성 식별
- **SHAP 값 분석**: 
  - 각 예측에 대한 특성 기여도 분석
  - 특성 간 상호작용 효과 시각화

#### 3. 예측 오차 분석
- **잔차 분석**: 
  - 예측 오차의 분포 확인
  - 특정 범위에서 오차가 큰지 확인
- **잘못 예측한 샘플 분석**: 
  - 오차가 큰 샘플의 특징 분석
  - 패턴 발견 및 특성 추가

#### 4. 검증 전략 개선
- **Hold-out Set**: 
  - 최종 검증을 위한 별도 Hold-out Set 확보
  - Public/Private Leaderboard 차이 예측
- **시간 기반 검증**: 
  - 데이터에 시간 정보가 있다면 시간 기반 검증

### 코드 구조 개선

#### 1. 모듈화 강화
- **Config 파일 분리**: 
  - 하이퍼파라미터, Feature Engineering 설정을 별도 파일로 관리
- **Pipeline 구조**: 
  - scikit-learn의 Pipeline 사용으로 전처리와 학습 통합

#### 2. 로깅 및 모니터링
- **자세한 로그**: 
  - 각 단계별 실행 시간 기록
  - 메모리 사용량 모니터링
- **실험 추적**: 
  - MLflow, Weights & Biases 등으로 실험 결과 추적

#### 3. 재현성 강화
- **Seed 고정**: 
  - 모든 랜덤 연산에 seed 설정
  - 환경별 결과 재현 가능하도록

### 성능 개선 우선순위

1. **높은 우선순위** (즉시 개선 가능):
   - Target Encoding 추가
   - 더 많은 상호작용 특성 생성
   - 특성 중요도 기반 특성 선택

2. **중간 우선순위** (시간 투자 필요):
   - Stacking 모델 추가
   - SHAP 값 분석으로 특성 이해도 향상
   - Hold-out Set으로 검증 전략 개선

3. **낮은 우선순위** (장기 개선):
   - Neural Network 모델 추가
   - 자동화된 Feature Engineering 파이프라인
   - 실험 추적 시스템 구축

## 📊 Feature Importance 분석

### 자동 분석 기능

모델 학습이 완료되면 **자동으로 Feature Importance 분석**이 수행됩니다.

**분석 내용**:
1. **모델별 Feature Importance**: CatBoost, LightGBM, XGBoost 각 모델의 특성 중요도 추출
2. **Fold별 평균**: 5-Fold CV의 모든 Fold에서 추출한 중요도의 평균 및 표준편차 계산
3. **시각화**: 모델별 상위 특성 비교 그래프 생성
4. **공통 중요 특성**: 모든 모델에서 공통으로 중요하게 판단한 특성 식별

### 생성되는 결과물

`feature_importance_results/` 디렉토리에 다음 파일들이 생성됩니다:

1. **CSV 파일**:
   - `catboost_feature_importance.csv`: CatBoost 모델의 특성 중요도
   - `lightgbm_feature_importance.csv`: LightGBM 모델의 특성 중요도
   - `xgboost_feature_importance.csv`: XGBoost 모델의 특성 중요도
   - `common_important_features.csv`: 모든 모델에서 공통으로 중요한 특성

2. **시각화 파일**:
   - `feature_importance_comparison.png`: 모델별 상위 특성 비교 (개별 그래프)
   - `feature_importance_combined.png`: 모든 모델의 상위 특성을 하나의 그래프로 비교

### Feature Importance 해석

**중요도 값의 의미**:
- 각 모델의 중요도는 **정규화**되어 합이 1이 됩니다.
- 높은 중요도 = 해당 특성이 예측에 더 많이 기여
- 표준편차(Std) = Fold 간 중요도의 변동성 (낮을수록 안정적)

**활용 방법**:
1. **특성 선택**: 중요도가 낮은 특성 제거 고려
2. **특성 Engineering**: 중요도가 높은 특성을 기반으로 새로운 특성 생성
3. **모델 해석**: 어떤 특성이 예측에 영향을 주는지 이해
4. **공통 중요 특성**: 모든 모델에서 중요하게 판단한 특성은 핵심 특성으로 간주

### Feature Importance와 상관관계 분석의 관계

- **상관관계 분석**: 선형 관계를 파악하는 **전처리 단계**의 분석
- **Feature Importance**: 실제 모델이 학습한 **비선형 관계 포함**한 중요도

**예시**:
- 상관관계가 낮지만 Feature Importance가 높은 특성 → 비선형 관계 또는 상호작용 효과
- 상관관계가 높지만 Feature Importance가 낮은 특성 → 다중공선성 문제 또는 중복 특성

두 분석을 **함께 고려**하여 최종 특성 선택을 결정하는 것이 권장됩니다.
